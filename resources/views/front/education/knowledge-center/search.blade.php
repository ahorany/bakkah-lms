{{--<div class="widget card box-shadow mb-4">
    <div class="card-header bg-primary text-white">
        <h4 class="mb-0">{{__('education.Search')}}</h4>
    </div>
    <div class="card-body">
        <form id="searchform" method="get" action="#">
            <input type="text" class="form-control search-field" name="s" placeholder="{{__('education.Search here')}}">
            <input type="hidden" name="lang" value="{{app()->getLocale()}}">
            <button type="submit" value="{{__('education.Search')}}"><i class="fas fa-search"></i></button>
        </form>
    </div> <!-- /.card-body -->
</div> <!-- /.widget -->
--}}
